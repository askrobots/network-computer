# network-computer: the connected device's camera as a webcam on the desk
# (nc-host -camera-device /dev/video10). exclusive_caps: browsers list it as a
# camera only while a device is sending, which is when there is a picture.
options v4l2loopback devices=1 video_nr=10 card_label="network-computer camera" exclusive_caps=1
