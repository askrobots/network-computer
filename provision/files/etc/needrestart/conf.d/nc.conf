# Never let package installs restart the desk's own services: restarting
# nc-xorg or nc-desktop closes every window of a session someone is using.
# Provisioning restarts them itself when their configuration changes.
$nrconf{override_rc}{qr(^nc-)} = 0;
