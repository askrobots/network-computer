# One apt package per line. Comments and blank lines ignored.
# Capture, encode, audio
ffmpeg
pulseaudio
pulseaudio-utils
pavucontrol
# record and play back sound (uses ALSA; the plugin routes ALSA to pulse, so it
# sees the phone microphone and plays to the desktop speaker)
audacity
libasound2-plugins
alsa-utils
# Headless desktop
xserver-xorg-core
xserver-xorg-video-dummy
xserver-xorg-input-evdev
xfce4
xfce4-terminal
xterm
dbus-x11
# GUI apps + polish
mousepad
xdotool
xclip
xinput
x11-xkb-utils
xdg-utils
# Tooling
git
curl
ufw
vlc
rofi
wmctrl
espeak-ng
python3-venv
conky-std
# the dashboard picks its layout by screen size (xdpyinfo); desktop icons are marked trusted (gio)
x11-utils
libglib2.0-bin
gir1.2-webkit2-4.1
# the object server's files as ~/Objects (WebDAV)
davfs2
# file dialogs for the dbbasic apps (Flutter's file_picker uses zenity on Linux)
zenity
# reading documents (Cabinet): text PDFs, scanned PDFs, images
poppler-utils
ocrmypdf
tesseract-ocr
# printing on the device you are connected from ("My device": CUPS + ncdevice)
cups
cups-filters
# the connected device's camera as a webcam (v4l2loopback is in the kernel modules)
v4l-utils
